{% extends 'base.html.twig' %}


{% block body %}
     <div class="container">
		<h1 class="fw-bold mb-0 text-black">All Bill</h1>
		<h6 class="mb-0 text-muted"></h6>
		<table class="table">
			<tr> 
			<th>No.</th>
			<th>Name Product</th>
				<th>Quantity</th>
				<th>Action</th>
				</tr> 
				{% for p in product %}
				<tr> 
				<td>{{p.id}}</td>
				<td>{{p.namep}}</td> 
				<td> 
				<input id="form1" min="0" name="quantity" value="{{p.quantity}}" type="number" class="form-control form-control-sm" />
			</td> 
			</tr>
			{% endfor %}
				</table>
				<h5>Total:${{total}}</h5>
				<hr class="my-4"> 
				<h6 class="mb-20" style="text-align:right "><a href="{{path('app_showall')}}" class="text-body">Back to shop</a></h6> 
				</form>
	
{% endblock %}